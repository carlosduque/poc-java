package o.patterns.state;

public class Main {
    public static void main(String[] args) {
        Saiyajin goku = new Saiyajin("Goku");

        goku.hit(300);
        goku.hit(430);
        goku.hit(320);

        goku.hit(380);
        goku.hit(110);
        goku.hit(450);
        goku.hit(680);
        goku.hit(830);
        goku.hit(250);
        goku.hit(360);
        goku.hit(920);
        goku.hit(120);
        goku.hit(210);
        goku.hit(640);
        goku.hit(380);
        goku.hit(110);
        goku.hit(450);
        goku.hit(680);
        goku.hit(830);
        goku.hit(250);
        goku.hit(360);
        goku.hit(920);
        goku.hit(380);
        goku.hit(110);
        goku.hit(450);
        goku.hit(680);
        goku.hit(830);
        goku.hit(250);
        goku.hit(360);
        goku.hit(920);
        goku.hit(120);
        goku.hit(210);
        goku.hit(640);
        goku.hit(380);
        goku.hit(110);
        goku.hit(450);
        goku.hit(680);
        goku.hit(830);
        goku.hit(250);
        goku.hit(360);
        goku.hit(920);
        goku.hit(120);
        goku.hit(210);
        goku.hit(640);
        goku.hit(380);
        goku.hit(110);
        goku.hit(450);
        goku.hit(1667);
        goku.hit(2);
    }
}
